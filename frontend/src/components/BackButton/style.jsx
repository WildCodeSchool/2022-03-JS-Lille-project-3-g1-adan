import styled from "styled-components";

export default styled.div`
  button {
    background-color: transparent;
    border: none;
    position: absolute;
    top: 2rem;
    left: 0.5rem;
    cursor: pointer;
  }
`;
